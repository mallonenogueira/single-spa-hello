{
  "name": "shared-dependencies",
  "scripts": {
    "start": "http-server --port 8081 --cors",
    "build": "rm -rf dist && mkdir dist && cp importmap.json dist/importmap.json"
  },
  "devDependencies": {
    "http-server": "^0.12.3"
  }
}
